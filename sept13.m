%example octave commands
%x = [-3 : .1 : 3];
%plot(x, x.^2);
%title('Example plot');

%this is an example octave script
%t=linspace(0, 2*pi, 50);
%x= cos(t);
%y= sin(t);

%plot the graph of a unit circle
%plot(x, y);
%grid on;

%if a>b
%  c=3
%else
%  c=4
%endif

%switch a
%  case 0
%    disp('a is zero')
%    case 1
%      disp('a is one')
%    otherwise
%      disp('a is not a binary digit');
%endswitch

f(x)=('x^2');

